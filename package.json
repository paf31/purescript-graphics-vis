{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build"
  },
  "devDependencies": {
    "pulp": "^11.0.2",
    "purescript": "^0.13.5",
    "purescript-psa": "^0.5.1",
    "rimraf": "^2.7.1"
  }
}
